# KAMIS - 농산물유통정보 (egg prices)
KAMIS_API_KEY=your_kamis_api_key_here
KAMIS_API_ID=your_kamis_id_here

# aT - 한국농수산식품유통공사 (feed prices)
AT_API_KEY=your_at_api_key_here

# Bank of Korea - 한국은행 (exchange rates)
BOK_API_KEY=your_bok_api_key_here

# KMA - 기상청 (weather data)
KMA_API_KEY=your_kma_api_key_here

# Database (PostgreSQL + TimescaleDB)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/eggprice

# Redis
REDIS_URL=redis://localhost:6379/0
CACHE_TTL_SECONDS=300

# Celery
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# JWT Authentication
JWT_SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
FROM_EMAIL=your_email@gmail.com

# OAuth (social login)
# Kakao: https://developers.kakao.com → 내 애플리케이션 → 앱 키
KAKAO_CLIENT_ID=your_kakao_rest_api_key
KAKAO_CLIENT_SECRET=your_kakao_client_secret
# Naver: https://developers.naver.com → Application → Client ID
NAVER_CLIENT_ID=your_naver_client_id
NAVER_CLIENT_SECRET=your_naver_client_secret
# Google: https://console.cloud.google.com → APIs & Services → Credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
# Frontend URL (OAuth 콜백 리다이렉트 대상)
FRONTEND_URL=http://localhost:3000

CORS_ORIGINS=http://localhost:3000
MODEL_VERSION=v2.0

# Security
ALLOWED_HOSTS=localhost,127.0.0.1
RATE_LIMIT_AUTH=10/minute
RATE_LIMIT_API=60/minute
RATE_LIMIT_STORAGE_URI=redis://localhost:6379/0

# Sentry (optional)
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=0.2

# AWS S3 (model storage, optional)
AWS_S3_BUCKET=
AWS_REGION=ap-northeast-2

# Retraining
MAPE_RETRAIN_THRESHOLD=7.0
RETRAIN_INTERVAL_DAYS=30
