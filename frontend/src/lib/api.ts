// Legacy API wrapper — replaced by lib/axios.ts + lib/queries.ts (React Query hooks)
// Kept as a re-export for any remaining direct usage during migration.
export { default as api } from "./axios";
